DATABASE_URL=postgresql://mouad:mouad@postgres:5432/geoinsight

SECRET_KEY=mouad

GEE_PROJECT_ID=geoinsight-501711

# Optional: path (inside the container) to a service-account JSON key.
# Leave unset to use the credentials from `earthengine authenticate` instead.
# GEE_SERVICE_ACCOUNT_FILE=/app/secrets/gee-service-account.json